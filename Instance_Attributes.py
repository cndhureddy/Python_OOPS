# In Dog() class, each dog has a specific name and age

class Dog:

    # Initializer / Instance Attributes
    def __init__(self, name, age):
        self.name = name
        self.age = age
        #  self variable is an instance of the class
